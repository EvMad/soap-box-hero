<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

</head>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">The Soap Box</a>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
               {{#if logged_out}}     <a class="nav-link active" aria-current="page" href="/login">Login</a>{{/if}}
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/createPost">Dashboard</a>
                </li>
                <li>{{#if logged_in}} <button type="button" class="btn btn-light" id="logout">Logout</button> {{/if}}</li>
            </ul>

        </div>
    </div>
</nav>

<body>{{{ body }}}</body>

{{#if logged_in}}
<script src="/logout.js"></script>
{{/if}}

</html>